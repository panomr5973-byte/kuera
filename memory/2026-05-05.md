# Memory Log - 2026-05-05

## Fase 7: Unified Desktop & Gateway Server

### Context
Setelah 23 hari jeda, muncul solusi untuk masalah yang diciptakan sendiri: terlalu banyak service berjalan sendiri-sendiri.

### Key Events
1. **`kuera_unified_desktop.py`** — Single-process orchestrator:
   - Flask Control Panel di port 7777.
   - Kelola 7 service: API (8000), Real API (8001), Web (5000), Dashboard (8501), Admin, Evolution, CLI.
   - Auto-restart crash (max 5x).
   - Unified log aggregation real-time.
   - Model registry integration (12 model, 29.45 GB).

2. **`gateway_server.py`** — WebSocket + HTTP gateway (port 18789):
   - Untuk KUERA Desktop/Wallpaper integration.
   - Token-based auth.
   - Heartbeat 20s.
   - Message history (max 50).

3. **`README_UNIFIED.md`** — Dokumentasi arsitektur unified.
4. **`start_kuera_unified.bat`** — One-click launcher.

### Decisions
- Semua service **mati secara default** untuk hindari konflik port.
- User aktifkan satu per satu dari Control Panel.
- Gunakan `ai_env/Scripts/python.exe` sebagai default interpreter.
- Browser auto-open ke localhost:7777.

### Issues
- Port conflict masih mungkin terjadi (5000 dipakai web + admin panel).
- `kuera_admin` dan `kuera_web_v2` sama-sama port 5000 — ini bug yang belum di-fix.
- Tidak ada test suite untuk unified desktop.

### Mood
Pragmatis. Bukan solusi elegan, tapi berfungsi. "Penyerahan diri yang produktif."

### Technical Debt
- HTML dashboard embedded sebagai string Python (759 baris) — sulit maintain.
- Process manager pakai subprocess.Popen + threading — tidak pakai async/await.
- Model registry hardcoded path: `models/llm/model_registry_active.json`.

### Next Target
- Fix port conflict.
- Hidupkan kembali memory system.
- Integrasikan context evolusi ke LLM.
